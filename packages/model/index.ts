export type ContactItem = {
	name: string;
	contact: number;
	insta: string;
	linkedin: string;
	image: string;
	role?: string;
	department?: string;
	email?: string;
};

export type SponsorItem = {
  name: string;
  image: string;
  url: string;
};

